[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/waste_keeper"]

[project]
name = "waste_keeper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openpyxl",
    "pywin32",
    "sqlmodel==0.0.39",
]

